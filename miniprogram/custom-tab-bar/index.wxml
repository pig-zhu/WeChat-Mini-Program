<!--miniprogram/custom-tab-bar/index.wxml-->
<view class="tab-bar">
  <!-- <cover-view class="tab-bar-border"></cover-view>
  <cover-view wx:for="{{list}}" wx:key="index" class="tab-bar-item" data-path="{{item.pagePath}}" data-index="{{index}}" bindtap="switchTab">
    <cover-image src="{{ item.iconPath}}"></cover-image>
    <cover-view style="color: {{item.color}}">{{item.text}}</cover-view>
  </cover-view> -->
   <mp-tabbar current="{{selected}}" style="position:fixed;bottom:0;width:100%;left:0;right:0;" list="{{list}}" bindchange="tabChange"></mp-tabbar>
</view>
